# Progress Log

[Phase 0.1] Created /backend, /web, /mobile, /prompts, /docs structure; initialized backend as a Python project (pyproject.toml with FastAPI/uvicorn/pytest/black/ruff); scaffolded /web and /mobile via `flutter create`; added root .gitignore (Python, Flutter/Dart, OS, editor artifacts). Assumption: no GNU `make` available on this machine and no admin rights to install it (chocolatey install failed with a permissions error), so `just` (installed via the `rust-just` PyPI wheel into the backend venv) is used instead of `make` throughout — noted in project memory. Assumption: no Android/iOS SDK or emulator available in this environment, so the mobile app's "hello world" DoD is verified via `flutter analyze` + `flutter test` (both pass) rather than an actual `flutter run`; the web app's DoD is verified via `flutter analyze` + `flutter test` + a full `flutter build web` (all pass), since Chrome is available as a build target. Backend "hello world" verified by actually booting `uvicorn` and curling `/health`. `pytest` exits 0 in /backend. — DoD verified.

[Phase 0.2] Added `app/main.py` with FastAPI app and `/health` endpoint (returns `{"status": "ok"}`, covered by a pytest test using TestClient), empty `app/routers/`, `app/services/`, `app/models/`, `app/ml/` packages, `black`/`ruff` config in `pyproject.toml`, and a `justfile` (in place of a Makefile, see 0.1 assumption) with `lint`, `format`, `test` targets. `GET /health` verified returning 200 with expected body. `just lint` and `just format` both run clean with no errors. — DoD verified.

[Phase 0.3] Added `.github/workflows/ci.yml` with a `backend` job (ruff check, black --check, pytest) and a `flutter` job (matrix over web/mobile: flutter analyze, flutter test), all third-party actions pinned to commit SHAs. YAML validated with a parser. Every command in the workflow was run locally and passes: `ruff check`, `black --check`, `pytest` (backend); `flutter analyze` + `flutter test` for both web and mobile. Each of backend/web/mobile has a trivial passing test (backend: `test_health.py`; web/mobile: the default `flutter create` counter widget test). — DoD verified.

[Phase 1.1] Wrote `backend/migrations/0001_core_tables.{up,down}.sql` creating `users`, `quizzes`, `questions`, `versions`, `submissions`, `answers` exactly as specified in CLAUDE.md Section 4. Applied to a local Supabase stack (`supabase start` in `/backend`, project_id `exam_scanner`, ports shifted to 54340-54342 — see below). Verified with a real script (`backend/tests/test_schema.py`, run via `just test`) that queries `information_schema.columns` and FK metadata and asserts an exact match against the spec — both tests pass. Verified the rollback: applied `0001_core_tables.down.sql`, confirmed `\dt public.*` shows zero tables, then re-applied the up migration with no errors either direction. Assumption/discovery: this machine already runs an unrelated, pre-existing Supabase Docker stack (project `quizapp`, a different ed-tech app of the user's) bound to the default local ports (54321-54327); rather than stopping it, our own stack's ports were shifted (documented in project memory) — verified read-only first that `quizapp`'s schema (`materials`, `skill_progress`, etc.) has nothing to do with this project before touching anything. — DoD verified.

[Phase 1.2] Enabled RLS on all 6 tables via `backend/migrations/0002_rls_policies.{up,down}.sql`: `quizzes` scoped to `owner_id = auth.uid()`; `questions`/`versions` scoped via their parent `quiz_id` → `quizzes.owner_id`; `submissions` via `version_id` → `versions.quiz_id` → `quizzes.owner_id`; `answers` via `submission_id` → that same chain; `users` scoped to `id = auth.uid()` (not explicitly specified in Section 4 but required since subtask 1.2 says RLS must be enabled on all 6 tables). Discovery: RLS policies alone weren't sufficient — Supabase's current default is that new tables aren't auto-exposed to the Data API, so PostgREST 403'd every request until explicit `grant select, insert, update, delete ... to authenticated` was added; this is now part of the up/down migration. Verified with a real integration test (`backend/tests/test_rls_cross_user.py`, 12 tests) that creates two actual Supabase Auth users, signs in to get real JWTs, and issues PostgREST requests — not raw SQL assertions. All 12 pass: user A gets zero rows across quizzes/questions/versions/submissions/answers/users when looking for user B's data, and gets their own rows back correctly in every table. — DoD verified.

[Phase 1.3] Wrote `backend/migrations/0003_seed_demo_data.sql`: one demo professor, one demo quiz with 5 MCQ questions, and 2 versions with valid `question_order`/`option_order` JSON, using fixed UUIDs and `ON CONFLICT ... DO UPDATE` for idempotency. Ran it twice against the local stack; row counts stayed at 1 professor / 1 quiz / 5 questions / 2 versions both times, no errors. Verified schema validity beyond "Postgres accepted the insert" with `backend/tests/test_seed_data.py` (4 tests): each version's `question_order` is an exact permutation of the quiz's 5 question IDs with no duplicates, each version's `option_order` covers every question with a valid index permutation matching that question's option count, `correct_option` is a valid letter for its options, and the `owner_id`/`quiz_id` FK chain resolves. All pass. — DoD verified.

[Phase 1] All three subtasks' DoD boxes verified above. Migration, rollback, and seed scripts committed under `/backend/migrations/` (`0001_core_tables.{up,down}.sql`, `0002_rls_policies.{up,down}.sql`, `0003_seed_demo_data.sql`, plus `verify_schema.sql` used for manual spot-checks). Note: the new integration tests (`test_schema.py`, `test_rls_cross_user.py`, `test_seed_data.py`) require a local Supabase stack (`supabase start` in `/backend`) and skip cleanly via `pytest.mark.skipif` when one isn't reachable — including in the current GitHub Actions CI, which doesn't stand up Docker/Supabase. They were run and verified locally against a real stack for every DoD check above; wiring them into CI (e.g. via `supabase/setup-cli` + `supabase db start`) is left as a follow-up, not required by this phase's DoD.

[Phase 2.1] Wrote `/docs/EXCEL_FORMAT.md`: 6-column layout (`question_text`, `option_a`-`option_d`, `correct_option`), positional (not header-name) parsing, validation rules, and a worked example table. Created 3 required fixtures under `/backend/tests/fixtures/excel/` (`valid.xlsx`, `missing_answer.xlsx`, `duplicate_options.xlsx`) plus one extra (`multiple_errors.xlsx`, used in 2.2 to prove multi-row error collection) via a committed generator script (`generate_fixtures.py`) so they're reproducible, not hand-crafted binary blobs. — DoD verified (files exist; used and passing in tests, see 2.2/2.3).

[Phase 2.2] Wrote `app/services/parsing.py`: `parse_quiz_excel(file) -> list[QuestionSchema] | ParseError`, using `openpyxl`. Validates per row: non-empty question text, non-empty + pairwise-distinct options, `correct_option` is exactly one of A/B/C/D (case-insensitive). Any row failure collects ALL of that row's failure reasons (not just the first) and ALL faulty rows across the file (not just the first bad row) — the function returns either the complete valid list or a `ParseError`, never a partial mix. Verified in `backend/tests/test_parsing.py` (4 tests): valid fixture asserts exact field values (not just count) for all 3 rows including a case-insensitive `"b"` → `"B"` normalization; the two required invalid fixtures assert exact row number + exact message string; a 4th test (`multiple_errors.xlsx`) proves two separate bad rows are both reported with their own distinct messages in one pass. — DoD verified.

[Phase 2.3] Added `POST /quizzes/{quiz_id}/upload` in `app/routers/excel.py`: parses the upload, 422s with the structured `ParseError` body on any invalid row (never a generic 500), 404s if the quiz doesn't exist, and on success inserts all questions via `app/services/questions.py::insert_questions` inside a single psycopg transaction (`with conn:` commits on success / rolls back the entire batch on any exception). Added `app/db.py` for the Postgres connection (defaults to the local Supabase dev DB from Phase 1; real deployments set `DATABASE_URL`). Verified in `backend/tests/test_upload_endpoint.py` (5 tests) against the real local Postgres: valid upload → 201 + exact row count in DB; both invalid fixtures → 422 with the exact structured error body + zero rows written; upload to a nonexistent quiz → 404 + zero rows written; and — going beyond "parsing already prevents partial writes" — a dedicated test that injects a genuine mid-batch DB failure via a monkeypatched `cursor.execute` (first row's INSERT actually runs, second is made to raise) and confirms the first row's insert is rolled back too, proving `insert_questions` is transactionally all-or-nothing, not just "we never attempted the write." — DoD verified.

[Phase 2] All three subtasks' DoD boxes verified above; full backend suite (28 tests) passes, `ruff`/`black --check` clean, `uvicorn` boots with both routes (`/health`, `/quizzes/{quiz_id}/upload`) registered. PROGRESS.md updated, code committed.

[Phase 3.1] Wrote `app/services/shuffler.py::generate_version(questions, version_number, seed)` and `generate_versions(questions, count, base_seed)`: Fisher-Yates via a seeded `random.Random` for both question order and, independently per question, option order; `option_order[question_id]` stores canonical option indices in shuffled order (matching CLAUDE.md Section 4). Added `app/models/version.py` (`QuestionForShuffle`, `ShuffledVersion`, `VersionCreateRequest`). Verified in `backend/tests/test_shuffler.py`: 10-question quiz / 5 versions produces 5 distinct `question_order`s; every version's option order is a programmatically-verified valid permutation for every question; correct-answer traceability manually verified end-to-end for 2 versions by recovering the true correct-answer text purely from the stored `option_order` mapping (no side-channel bookkeeping). — DoD verified.

[Phase 3.2] Added the anti-clustering reroll pass inside `generate_version`: rerolls (up to `MAX_RESHUFFLE_ATTEMPTS = 200`) while the correct-answer letter distribution exceeds `CORRECT_LETTER_MAX_FRACTION = 0.5` (both module-level constants, not hardcoded inline), and `generate_versions` additionally rerolls on an exact-distribution repeat across versions in the same batch via a shared `previous_distributions` set. Verified in `test_shuffler.py`: 12-question quiz / 20 versions — no version exceeds the configured threshold on any letter; a dedicated single-question-quiz test (where >50% clustering is mathematically unavoidable) proves the reroll loop terminates deterministically at `max_attempts` rather than hanging (call count on the shuffle helper asserted `== max_attempts` via a patched counting wrapper), plus a companion test proving the loop exits early (well under `max_attempts`) when clustering is easily avoidable. — DoD verified.

[Phase 3.3] Added `POST /quizzes/{quiz_id}/versions` (`app/routers/versions.py`) accepting `{count}`, 400s if `count < 1` or the quiz has zero questions, 404s if the quiz doesn't exist; `app/services/versions.py::get_questions_for_shuffle` + `insert_versions` (single transaction, `qr_id` populated with a placeholder unique UUID string — real QR image content is Phase 4's job, this column only needs uniqueness now). No update/delete endpoint exists for versions by construction. Verified against the real local Postgres in `backend/tests/test_versions_endpoint.py` (6 tests): requesting 5 versions for a real 6-question quiz creates exactly 5 rows, each independently re-verified against the Subtask 3.1/3.2 checks (valid permutations, exact question-id coverage, clustering threshold, unique `qr_id` per version) by re-reading them back from the DB; 0-question quiz and `count < 1` both 400 with zero rows written; nonexistent quiz 404s; and immutability is checked two ways — live HTTP PUT/PATCH/DELETE attempts against a created version all return 404/405, plus a route-table introspection test (`_iter_api_routes`, walking into FastAPI's `_IncludedRouter.original_router` since FastAPI 0.139's lazy router wrapping otherwise makes such a check trivially vacuous) confirming no registered route touching `versions` exposes PUT/PATCH. Also manually booted `uvicorn` and hit `POST /quizzes/{fake-id}/versions` live, confirming the real structured 404 body (not a generic ASGI 404) — the two clients agree. — DoD verified.

[Phase 3] All three subtasks' DoD boxes verified above. Full backend suite (45 tests) passes, `ruff`/`black --check` clean. PROGRESS.md updated, code committed.

[Phase 4.1] Added `app/services/qr.py`: `generate_qr(qr_id) -> PNG bytes` (via `qrcode`, error-correction level M) and `decode_qr(image_bytes) -> str | None`. Discovery: OpenCV's `cv2.QRCodeDetector` (the tech-stack's first-listed option) reliably failed to decode these locally-generated PNGs on this machine (0/20 decoded) even at full resolution; switched to `pyzbar` (the documented alternative in CLAUDE.md Section 3), which decoded 20/20 round-trips with zero failures — noted here since a future OMR pass (Phase 5/6) reusing QR decoding should default to `pyzbar`, not OpenCV's built-in detector. Verified in `backend/tests/test_qr.py`: 20 random UUIDs generate + decode to an exact string match; a corrupt/blank image decodes to `None` rather than raising. — DoD verified.

[Phase 4.2] Added `app/services/pdf_gen.py::render_version_pdf(quiz_title, version, questions_by_id) -> PDF bytes` (ReportLab, `canvas.Canvas(..., invariant=1)` so identical logical input always produces byte-identical PDFs — no embedded timestamp/doc-id variance) and `app/config/pdf_template.json`, the documented single source of truth for all layout geometry (page size, fixed QR box, question-row grid, bubble-center formula) consulted by `pdf_gen.py` now and by OMR alignment in Phase 5/6. Design decision (multi-page QR strategy): the QR code (same `qr_id`) is re-embedded at the identical position on *every* page, not just page 1, because a scanned page can be out of order or physically separated from the rest of its packet — documented in `pdf_gen.py`'s module docstring. Verified in `backend/tests/test_pdf_gen.py` (8 tests) using PyMuPDF to rasterize/introspect the rendered PDF: a 10-question/4-option quiz renders to exactly 1 page with every question/option/bubble-label string extractable from the page text; a `questions_per_page + 4`-question quiz splits across exactly 2 pages at the expected boundary; QR bounding boxes are pixel-identical (`pytest.approx`, 0.1pt tolerance) across 3 independently generated sample PDFs; a monkeypatch test proves the QR position actually moves when the template config's `qr_box` values change (proving the geometry is read from the config file, not hardcoded); and render output is verified byte-identical across two calls with identical input. Manually rendered and visually inspected one 10-question PDF page screenshot (150 DPI) before writing the automated checks. — DoD verified.

[Phase 4.3] Added `app/services/storage.py`: talks to the local Supabase Storage REST API directly over `httpx` with the service-role key (same trusted-backend pattern as `app/db.py`'s direct Postgres connection, rather than pulling in the full `supabase-py` SDK), bucket `version-pdfs`, object path `{quiz_id}/{version_id}.pdf`. Added `GET /versions/{version_id}/pdf` in `app/routers/versions.py`: 404s if the version doesn't exist; otherwise checks `storage.object_exists` first and only renders+uploads on a cache miss, returning a signed download URL either way. Discovery: local Supabase Storage ships disabled by default (`[storage] enabled = false` in `supabase/config.toml`); flipped it on and restarted the stack. Verified against the real local Postgres + Storage stack in `backend/tests/test_pdf_storage_endpoint.py` (3 tests): after hitting the endpoint, the signed URL is independently fetchable and its bytes match both a fresh direct call to `render_version_pdf` with the same DB-loaded inputs and a direct `storage.download_pdf` of the same object (byte-for-byte, not just same size); a dedicated idempotency test monkeypatches both `render_version_pdf` and `storage.upload_pdf` with call-counting wrappers and asserts each is invoked exactly once across three successive downloads of the same version, with all three responses' fetched bytes identical; a nonexistent version 404s. Also manually booted `uvicorn` and hit the live endpoint for a nonexistent version, confirming the real 404 body. — DoD verified.

[Phase 4] All three subtasks' DoD boxes verified above, including the documented bubble-coordinate template config (`app/config/pdf_template.json`) that Phase 5/6's OMR alignment will depend on. Full backend suite (58 tests) passes, `ruff`/`black --check` clean. New runtime dependencies added to `pyproject.toml`: `qrcode[pil]`, `reportlab`, `pyzbar`, `PyMuPDF` (test-only use, but kept as a direct dependency since OMR alignment work will need PDF/image introspection again), `httpx` (moved from dev-only to a runtime dependency since `app/services/storage.py` uses it at request time). PROGRESS.md updated, code committed.

[Phase 5.1] Added a `fiducials` block to `app/config/pdf_template.json` (4 solid black 22pt corner squares, 12pt margin from each edge) and `_draw_fiducials` in `pdf_gen.py` to render them as vector rects (not raster images, so Phase 4's "QR is the only embedded raster image on the page" test assertions still hold unmodified) on every page. Factored the bubble-center and new fiducial-position math out of `pdf_gen.py` into `app/services/geometry.py` (`bubble_center_pt`, `fiducial_positions_pt`/`fiducial_centers_pt`, `pdf_point_to_pixel`, `qr_box_pixel_rect`) so PDF rendering and OMR alignment share one coordinate source of truth instead of drifting. Added `app/ml/omr/align.py::align_page(image, template, dpi) -> AlignmentResult`: detects the 4 fiducials via OpenCV contour/shape filtering (rotation-invariant `minAreaRect`-based extent/aspect checks, not axis-aligned-bbox ones, which would falsely reject a rotated square), computes a homography via `cv2.getPerspectiveTransform`, and warps the scan back to canonical page geometry. Discovery/design decision: the fiducial size (22pt) was deliberately chosen well above this project's QR finder-pattern outer-square size (~17pt, computed from the actual `qrcode` module count at ERROR_CORRECT_M for a UUID-length `qr_id`) — a naive detector otherwise confuses the two, since a QR finder pattern's outer ring is geometrically a solid square to `cv2.findContours`. Also discovered and fixed a real correspondence-ambiguity bug: a rectangle is symmetric under 180-degree rotation, so a candidate-to-corner assignment that just minimizes similarity-transform residual is a coin flip between the true match and its 180-degree-flipped twin (both fit near-perfectly) — fixed by breaking ties among near-equal-residual assignments in favor of the smallest absolute recovered rotation angle, since a real scan is never rotated anywhere near 180 degrees. Verified in `backend/tests/test_align.py` (6 tests) using synthetically rotated (5°/15°/30°, `cv2.warpAffine` with expanded white-padded canvas) renders of a real generated version PDF (rasterized via PyMuPDF, no fixture image files needed): recovered bubble-center pixel coordinates are within a defined ±3px tolerance at all three angles (actual observed error ~0.8px, well inside tolerance) against a forward-computed ground truth (the known rotation matrix applied to the template's true bubble coordinates — not circular, since only the 4 fiducial corners feed the homography fit); a dedicated occlusion test whites out one corner's fiducial region and asserts `AlignmentResult(success=False, error=...)` with no exception and no warped image, plus a companion test with all 4 fiducials removed. Full backend suite (32 passed, 32 skipped needing local Supabase) still green, `ruff`/`black --check` clean. New dependencies added to `pyproject.toml`: `opencv-python-headless`, `scikit-learn`, `numpy`, `joblib` (installed into `backend/.venv`, the project's actual venv — the system `python` on this machine is a different, unrelated interpreter without the project's packages, noted here since it tripped up test discovery once). — DoD verified.

[Phase 5.2] Added `app/ml/omr/extract.py::extract_bubble_crops(aligned_image, template, num_questions_on_page, num_options, dpi) -> list[BubbleCrop]`, cropping each bubble from an already `align_page`-corrected image using the same `bubble_center_pt`/`pdf_point_to_pixel` geometry helpers PDF rendering and alignment already share, with a small configurable padding (`CROP_PADDING_PT`) around the bubble radius so a fill or stray mark isn't clipped at the crop edge. Verified in `backend/tests/test_extract.py` (4 tests, all against real rendered-then-rasterized PDF pages, no fixture images): an 8-question/4-option page yields exactly 32 crops; three differently-shaped sample pages (8x4, 6x3, 10x5 questions/options) each have specific bubbles marked "filled" with a directly-drawn solid dot (simulating a student's pencil mark) before cropping, and the test asserts every marked bubble's crop is strictly darker than every unmarked crop on that page — this would fail if crop coordinates were off by even a few pixels, since the mark would land partially or fully outside the crop. All crops from all 3 sample pages were additionally saved as PNGs to `backend/tests/test_output/bubble_crops/{sample}/` and manually visually inspected (filled crops show a solid black disc centered inside the printed ring; empty crops show only the ring) before locking in the darkness assertion, per the DoD's explicit "visually verified... then lock in the assertion" requirement. Full backend suite (36 passed, 32 skipped needing local Supabase) green, `ruff`/`black --check` clean. — DoD verified.

[Phase 5.3] Bubble fill classifier, split per Section 5's `/app/ml/bubble_classifier/` (training) vs. `app/ml/omr/classify.py` (inference) structure. TRAINING DATA: entirely synthetic (documented in `app/ml/README.md`, written alongside this subtask) — no real scanned exam data exists yet; `app/ml/bubble_classifier/synthetic_data.py` programmatically renders bubble crops (varying fill %, Gaussian noise, small random rotation) since none was available, per the prompt's explicit placeholder-data allowance. Started with the simplest model per the prompt's "start with the simplest model that clears the bar" instruction: `app/ml/bubble_classifier/features.py::extract_features` computes 5 pixel-darkness features (mean/std/dark-fraction over the whole crop, plus separate inner-disc vs. outer-ring darkness means) from a DPI-independent, resized-to-32x32 crop; `train.py` fits an `sklearn.linear_model.LogisticRegression` on these — this cleared the accuracy bar immediately (held-out accuracy 1.0000 on a 560/240 stratified split) so no escalation to a CNN was needed. `classify.py::classify_bubble(crop) -> (label, confidence)` loads the saved `model.joblib` artifact (committed to the repo, 911 bytes) and returns `"ambiguous"` whenever `max(predict_proba) < CONFIDENCE_THRESHOLD` (0.85), independent of which class it leaned toward. Discovery during ambiguous-case tuning: the first-draft synthetic "partial erasure" (small 0.4x-bubble-radius erasure) and "light mark" (fixed 0.4 attenuation) cases weren't actually landing near the classifier's decision boundary — they were still confidently (and correctly) called filled/empty, which happens to satisfy "never confidently wrong" but not the DoD's "correctly routed to low confidence" bar. Fixed by tuning both generators (erasure radius 0.6x bubble radius; attenuation randomized 0.45–0.6) via empirical iteration against the actual trained classifier until routing rates were verified, not assumed. Verified in `backend/tests/test_bubble_classifier.py` (11 tests): held-out accuracy ≥95% (actual 100%) on a freshly-trained model each run; a second independently-seeded unambiguous set has zero confident misclassifications; three dedicated ambiguous kinds (mid-range fill, light/faint mark, heavily partially-erased) each hit ≥90% ambiguous-routing (actual 100% each); a 4th kind (stray mark near an otherwise-empty bubble) is asserted to never be confidently mislabeled "filled" (the actual risk that case guards against, distinct from "must be ambiguous"); every returned label's confidence is cross-checked against `CONFIDENCE_THRESHOLD` for consistency; decoupling is proven by AST-walking `classify.py`'s actual import statements (not a substring search — its own docstring legitimately contains the word "trained") to confirm it imports neither the training module nor scikit-learn's training APIs, then clearing its model cache and re-classifying to confirm it reloads cleanly from the on-disk artifact alone. Full backend suite (47 passed, 32 skipped needing local Supabase) green, `ruff`/`black --check` clean. — DoD verified.

[Phase 5.4] Added `app/ml/omr/qr_decode.py::decode_qr_from_page(page_image, template, dpi) -> str | None`: crops the page's fixed QR box (via `geometry.qr_box_pixel_rect`, the same top-left-origin pixel-rect math as Phase 4's own `_crop_qr` test helper) and reuses the already-tested `pyzbar`-backed `app/services/qr.py::decode_qr` rather than duplicating zbar-calling logic — deliberately re-verifying `pyzbar` over OpenCV's `QRCodeDetector` per the Phase 4.1 discovery. Returns `None` (never raises) on an empty/out-of-bounds crop or a failed `cv2.imencode`. Verified in `backend/tests/test_qr_decode.py` (23 tests): 20 parametrized samples, each a distinct rendered version (distinct `qr_id`) rasterized then passed through a simulated-photo pass (Gaussian blur + lossy JPEG recompression at quality 75, standing in for "rendered-then-photographed" per the DoD's own suggested substitute) — all 20 decode to the exact original `qr_id` string; a deliberately-occluded case (QR box whited out entirely) and a heavily-blurred case (31x31 Gaussian kernel over just the QR region) both return `None` cleanly; an out-of-bounds/too-small input image also returns `None` rather than raising. Full backend suite (70 passed, 32 skipped needing local Supabase) green, `ruff`/`black --check` clean. — DoD verified.

[Phase 5] All four subtasks' DoD boxes verified above. Added `app/ml/README.md` documenting the bubble classifier's training data source (entirely synthetic — no real scanned exams exist yet), measured accuracy (100% held-out, 100% ambiguous-routing, both above their DoD targets), and known limitations (synthetic-only data needing real-scan retraining before production use; single-template assumption; the fiducial/QR-finder-pattern size margin that must be re-checked if the template changes; no lighting/exposure normalization). Explicitly re-verified (via `grep` across all Phase 5 code/docs) that no LLM API, key, or client library is referenced anywhere in `app/ml/` — every step is deterministic OpenCV geometry (`align.py`, `extract.py`, `qr_decode.py`) or a small trained `scikit-learn` classifier (`bubble_classifier/`), per CLAUDE.md Section 2. Full backend suite (70 passed, 32 skipped needing a local Supabase stack unrelated to this phase) green, `ruff`/`black --check` clean, all four subtasks committed individually. New dependencies (`opencv-python-headless`, `scikit-learn`, `numpy`, `joblib`) added to `pyproject.toml` in 5.1.

[Phase 6.1-6.3] Added `app/models/scoring.py` (`VersionLookupResult`, `DetectedAnswer`, `ScoredAnswer`, `ScoringResult`) and `app/services/scoring.py`: `lookup_version_by_qr_id(conn, qr_id) -> VersionLookupResult | None` (6.1, returns `None` rather than raising on a foreign/corrupted QR), and `translate_and_score(lookup, detected_answers) -> ScoringResult` (6.2/6.3), which un-shuffles each detected bubble position through *both* `question_order` and `option_order` independently (`question_order[position]` recovers the canonical question; `option_order[question_id][shuffled_index]` recovers the canonical option), scores against `questions.correct_option`, and applies the CLAUDE.md Section 2 rule-5 confidence gate: zero marks, multiple marks, or any classifier-flagged-`"ambiguous"` bubble sets that answer's `flagged=true` and excludes it from scoring; a submission is `"finalized"` only if zero answers are flagged, else `"needs_review"` with `total_score = None`. Verified in `backend/tests/test_scoring.py` (10 tests) against the real local Postgres and the actual Phase 1.3 demo seed data (version `demo-quiz-v1`, whose `question_order` and *every* question's `option_order` are non-identity permutations — the hardest case, not the trivial one): valid/nonexistent `qr_id` lookup (6.1); all-correct/all-wrong/mixed detected-answer sets each matched against an independently hand-derived expected score (3+ constructed sets, hand-derivation done via the same `.index(canonical_index)` pattern `test_versions_endpoint.py` already uses to verify shuffle output, not by calling any scoring code) (6.2); a dedicated test isolates one question whose `option_order` is neither identity nor a fixed offset and confirms the exact canonical letter recovered; all-clean → `finalized`, one multi-mark / one zero-mark / one classifier-ambiguous case each → `needs_review` with *exactly* that question's answer flagged and every other answer unaffected (6.3). Full backend suite green, `ruff`/`black --check` clean. — DoD verified.

[Phase 6.4] Added `app/services/submissions.py` (`create_submission`, `get_submission_with_answers`, `list_submissions_by_status`, `apply_manual_correction`) and `app/routers/scan.py`: `POST /scan` runs the full pipeline (decode uploaded image bytes → `decode_qr_from_page` → `lookup_version_by_qr_id` → `align_page` → `extract_bubble_crops` → `classify_bubble` per bubble → `translate_and_score` → persist submission+answers), `GET /submissions?status=` for the review queue, `PATCH /submissions/{id}/answers/{id}` for a professor's manual correction (recomputes that answer's `correct`/`score` against the quiz's master key via a `submissions→versions→questions` join, un-flags it, and flips the submission to `finalized` + recomputes `total_score` once every answer is resolved). Distinct failure modes, none silently folded into `needs_review`: unreadable QR → 422 before version lookup or scoring are ever reached (no submission row created); QR from an unregistered version → 404; a version spanning more than one printed page → 501 (documented scope limit — no multi-page-submission stitching yet; every question is assumed to have exactly 4 options per the fixed Excel upload format, so `NUM_OPTIONS` isn't derived per-question). Bug caught and fixed during this subtask: `apply_manual_correction` initially wrapped its writes in `with conn:`, which in psycopg3 closes the underlying connection (not just the transaction) on exit — broke the read-back immediately after; fixed by using explicit `conn.commit()` so the caller's connection stays open, documented in the function's docstring so the mistake isn't repeated. Verified end-to-end in `backend/tests/test_scan_endpoint.py` (6 tests) against the real local Postgres with **no mocking of alignment/extraction/classification**: a real version PDF rendered via the Phase 4 renderer, rasterized via PyMuPDF, marked with actual pencil-style filled dots at computed bubble coordinates, and POSTed as PNG bytes through the live `/scan` route — all-correct → `finalized` with the exact hand-calculated `total_score` and matching DB state; one multi-mark row → `needs_review` with exactly that question flagged; QR box whited out → 422 `qr_unreadable`, a monkeypatch asserts `lookup_version_by_qr_id` is never even called, and a DB count proves zero submissions were created; a real QR for an unregistered `qr_id` spliced into an otherwise-valid page → 404 `version_not_found`; a professor's `PATCH` on the flagged answer → answer un-flagged with recomputed `correct`/`score`, submission flips to `finalized` with the recomputed `total_score`, confirmed both in the response body and by re-querying the DB; `GET /submissions?status=` correctly includes/excludes the submission by status. Full backend suite (118 passed, 0 skipped — a local Supabase/Postgres stack was started for this session) green, `ruff`/`black --check` clean.

[Phase 6] All four subtasks' DoD boxes verified above via real `pytest` runs against a live local Postgres (not assumed). `main.py` wires in the new `scan` router alongside `excel`/`versions`. PROGRESS.md updated, code committed.

[Phase 7 prep] Discovery: none of Phase 7's assumed backend surface for quiz CRUD actually existed - `app/routers/excel.py`/`versions.py` only accept an already-existing `quiz_id`, with no endpoint to create or list a professor's own quizzes, and `app/db.py` explicitly deferred ownership enforcement to "once auth is wired in (later phase)". Since the web app can't function without these, added them here rather than blocking on Phase 7's frontend-only framing: `app/services/auth.py` (`get_current_user` FastAPI dependency verifying a Supabase-issued bearer JWT; `ensure_user_row` lazily upserts into `public.users`, a separate table from Supabase's own `auth.users`) and `app/routers/quizzes.py` (`POST /quizzes`, `GET /quizzes` scoped to `owner_id = current_user.id`, `GET /quizzes/{id}/versions`). Added CORS middleware to `main.py`, scoped to `localhost`/`127.0.0.1` via `allow_origin_regex` (not a `*` wildcard - flagged by an automated security check) since the Flutter web dev server runs on a random localhost port. Discovery: this local Supabase stack signs JWTs with ES256 via its JWKS endpoint (the newer Supabase "signing keys" feature), not the legacy shared-secret `JWT_SECRET` - verified by decoding a real token's header and cross-checking `/auth/v1/.well-known/jwks.json`; verification uses `PyJWT`'s `PyJWKClient` instead, which also works against a real hosted Supabase project unmodified. Bug caught via a 15-iteration stress test of the new decode path: intermittent `ImmatureSignatureError` (`iat` briefly in the future) from clock skew between this process and the Dockerized Supabase Auth container - fixed with `leeway=10` on `jwt.decode`, re-verified with another 15-iteration run (0 failures) plus 3 consecutive full-suite runs. Also hit and fixed the same psycopg3 `with conn:`-closes-the-connection gotcha already documented for Phase 6.4's `apply_manual_correction` (this time in `ensure_user_row` closing the connection before the calling route could use it) - fixed with explicit `conn.commit()`. Verified in `backend/tests/test_quizzes_endpoint.py` (5 tests) against the real local Supabase + Postgres stack with two distinct real Supabase Auth users: create+list roundtrip, cross-professor list isolation (one professor never sees another's quiz titles), 404 (not a leaked 401) on a nonexistent-or-foreign quiz's versions, and version listing after real generation via the Phase 3 shuffler. Full backend suite (123 passed, up from 118) green, `ruff`/`black --check` clean. New dependency: `pyjwt[crypto]` (added to `pyproject.toml`).

[Phase 7 prep, cont.] Added two more backend gaps discovered while building the results dashboard and review screens: `GET /quizzes/{id}/submissions` (per-quiz, mixed statuses, optional status filter - the existing `list_submissions_by_status` was single-status/all-quizzes, wrong shape for a per-quiz dashboard) and `GET /submissions/{id}` (single-submission detail with answers - the only prior read path was PATCH's response, which only exists *after* a correction, too late for a screen that needs to show what to correct). Verified in `backend/tests/test_quiz_submissions_endpoint.py` (5 tests). A background automated security review then flagged broken access control: the new `GET /submissions/{id}` had no auth at all, and PATCH `.../answers/{id}` (shipped auth-free in Phase 6.4) let anyone read or correct any professor's student answers by guessing a submission UUID - both are exactly what the new review screen calls, and it already sends a bearer token on every request, so closing this needed no frontend change. Added an ownership check (`submissions -> versions -> quizzes.owner_id`) to both routes; updated `test_scan_endpoint.py`'s `seeded_quiz` fixture to use a real Supabase Auth user (it previously used a bare `users`-table row with no way to authenticate as it) so its PATCH test could supply a real token. Full backend suite (129 passed) green, `ruff`/`black --check` clean.

[Phase 7.1] Auth & shell: `lib/main.dart` initializes `supabase_flutter` against the local dev project (URL/publishable key in `lib/config.dart`, overridable via `--dart-define` for a real deployment); `lib/providers/auth_providers.dart` bridges Supabase's `onAuthStateChange` stream into riverpod; `lib/router/app_router.dart` (go_router) redirects unauthenticated users to `/login` and authenticated users away from `/login`/`/signup`, driven by a `GoRouterRefreshStream` so the guard reacts immediately to sign-in/out, not just on next navigation. `lib/screens/shell/app_shell.dart` gives the Quizzes/Results/Account `NavigationRail`. DoD verified with a real local Supabase project (not a mock) in `web/test/auth_flow_test.dart` (4 widget tests): sign up, log in, log out, and an explicit unauthenticated-redirect check, all pumping the actual app and tapping its actual screens. Major tooling discovery, since this pattern recurs through every Phase 7.x test: `flutter_test`'s widget-test binding (a) installs a fake `HttpOverrides` that fails all real requests with 400, worked around per-test via `test/support/test_env.dart::withRealNetwork` (clears `HttpOverrides.global` for the test's duration - an earlier version using `HttpOverrides.runZoned` with one shared client broke on a second network call, since some request paths `.close()` their client when done); (b) runs the *entire* test body inside a `FakeAsync` zone, so a real I/O completion's continuation - scheduled via `scheduleMicrotask` in that zone - never resolves on its own; `tester.runAsync()` is required, and critically must wrap the *triggering* `tester.tap()`/`pumpWidget()` itself, not just a delay called afterward - a delay called after an already-dispatched tap does not retroactively un-stick that tap's stuck continuation (confirmed by a signup button sitting on its spinner through a full 2-second post-tap real wait, until the tap itself was moved inside `runAsync`); (c) `tester.pumpAndSettle()` never terminates against this app's perpetually-animating `CircularProgressIndicator` loading states - hung indefinitely on first attempt - replaced everywhere with a bounded loop of fixed-duration `tester.pump()` calls (`pumpBounded`/`tapAndAwaitRealNetwork` helpers); (d) `SharedPreferences.setMockInitialValues({})` needed before `Supabase.initialize` (no platform channel behind it in tests); (e) `detectSessionInUri: false` needed in `FlutterAuthClientOptions` to stop a stray `app_links` `MissingPluginException` (no OAuth/deep-link flows are used) from being counted as a test failure. Full `flutter analyze` clean (2 pre-existing info-level notices only).

[Phase 7.2] Quiz creation flow: `lib/screens/quizzes/quiz_create_screen.dart` - title → `POST /quizzes` → upload Excel (`file_picker`, injectable as `pickFile` for testability) → `POST /quizzes/{id}/upload`, surfacing every row's messages from a 422 `ExcelValidationException` (not just the first) → version count → `POST /quizzes/{id}/versions`. `lib/screens/quizzes/quizzes_list_screen.dart` lists a professor's own quizzes (`GET /quizzes`). Verified end-to-end against the real backend in `web/test/quiz_creation_flow_test.dart` (2 widget tests, real Supabase + real FastAPI, fixtures read from `backend/tests/fixtures/excel/`): uploading `multiple_errors.xlsx` shows both bad rows (2 and 3) and every one of their distinct reasons, matching `backend/tests/test_parsing.py`'s known expected content exactly; uploading `valid.xlsx` end-to-end reaches "3 version(s) generated" with a working "View versions" link. — DoD verified.

[Phase 7.3] Version download: `lib/screens/quizzes/quiz_versions_screen.dart` lists versions with a per-version download button; actual byte download is a plain injectable callback (`downloadBytes`), with the real implementation (`lib/services/download/download_web.dart`, browser Blob+anchor-click) behind a conditional export (`download.dart` picks the web impl only under `dart.library.html`, a no-op stub otherwise) so `flutter test`'s VM runtime never tries to load `dart:html`. Verified in `web/test/version_download_test.dart` against the real backend: downloads 3 real versions of a real quiz, hashes match a fresh direct re-download of each (byte-for-byte, proving the storage cache-on-miss path), and all 3 hashes are pairwise distinct. Discovery: the first download of each version is slow (real PDF render + storage upload on cache miss, per Phase 4's design) - needed a 3-second real wait per download tap, not the ~800ms sufficient for an already-cached fetch.

[Phase 7.4] Results dashboard: `lib/screens/results/results_screen.dart` - quiz selector + status filter (all/pending/needs_review/finalized) + sortable-by-time list, backed by `GET /quizzes/{id}/submissions`. Verified in `web/test/results_dashboard_test.dart` against a directly-seeded (docker-exec SQL, mirroring the backend test suite's own pattern) set of 3 submissions with distinct statuses on the real backend: exact status/score text per row (not just "a list renders"), and filtering to `needs_review` leaves exactly that one row. Also fixed a real bug caught by this test's flakiness: `apiClientProvider` never closed its `http.Client`, leaving a dangling keep-alive timer that tripped `flutter_test`'s teardown invariant check - fixed with `ref.onDispose(client.close)`.

[Phase 7.5] Flagged-answer review: `lib/screens/results/submission_review_screen.dart` shows each flagged answer (question, detected option, confidence) with a dropdown to select the correct option and a per-answer Save button calling `PATCH /submissions/{id}/answers/{id}`; local widget state (not a re-fetch) is updated from each PATCH response and propagated to the results dashboard's provider via `updateSubmissionLocally`, giving the reactive-without-refresh behavior the DoD requires. Verified in `web/test/submission_review_test.dart` against the real backend with a submission seeded with 2 flagged answers: resolving the first leaves the submission `needs_review` with "1 flagged question(s) remaining" (the explicit multi-flag boundary - it must not finalize early); resolving the second flips it to finalized with the correct total score, reflected in the same pumped widget tree. This test caught a real bug: `ApiClient.getSubmission()` never attached the auth header at all (plain oversight - every other method does) - the backend correctly 422'd on the missing required header; fixed and re-verified. Also caught and fixed a test-fixture bug in the process: the seed SQL reused a fixed placeholder email and a fixed `qr_id` across runs, both unique-constrained columns, so a second run collided with the first run's never-cleaned-up rows - fixed by making both per-run-unique and adding real teardown for the seeded quiz/questions/versions/submissions/answers.

[Phase 7] All five subtasks' DoD boxes verified above via real `flutter test` runs against the real local Supabase + FastAPI stack (no mocked backend anywhere) - `web/test/{auth_flow,quiz_creation_flow,version_download,results_dashboard,submission_review}_test.dart`, 9 tests total, all passing individually and together in one `flutter test` run. `flutter analyze` clean (2 pre-existing info-level notices: a `prefer_const_constructors_in_immutables` suggestion and the expected `avoid_web_libraries_in_flutter` notice on the conditional-export download helper). `flutter build web` succeeds. New dependencies added to `web/pubspec.yaml`: `supabase_flutter`, `flutter_riverpod`, `go_router`, `http`, `file_picker`, `crypto`, `intl` (runtime); `mocktail`, `uuid`, `shared_preferences` (dev/test). Two real application bugs were caught and fixed purely by writing these tests against the real stack rather than mocks: a missing auth header on one API call, and a dangling HTTP client leaking a keep-alive timer. PROGRESS.md updated, code committed per subtask.

[Phase 8.1] Auth: `lib/screens/auth/login_screen.dart` (sign-in only, no signup - professors register on web per CLAUDE.md) + `lib/providers/auth_providers.dart` + `lib/router/app_router.dart` (go_router redirect guard, same `GoRouterRefreshStream` pattern as web) reuse the same Supabase project as web (`lib/config.dart` mirrors `web/lib/config.dart`'s defaults). DoD verified against the real local Supabase project in `mobile/test/auth/login_flow_test.dart`: a professor account created via the same Supabase Auth backend the web app's signup screen calls (both clients point at the identical default local project) signs in successfully on mobile and lands on the protected `/scan` route; a second test confirms an unauthenticated session is redirected to `/login`. Reused the same three `flutter_test` real-network workarounds already documented for Phase 7.1 (fake `HttpOverrides`, `FakeAsync`-trapped real awaits, mocked `SharedPreferences`) via `mobile/test/support/test_env.dart`, adapted from `web/test/support/test_env.dart`. — DoD verified.

[Phase 8.2] Camera capture blur/QR pre-check: `lib/services/image_quality.dart`'s `ImageQualityChecker` is a pure, deterministic (no LLM, no network - CLAUDE.md Section 2) fast local pre-check - variance-of-Laplacian for blur, pixel-intensity spread for "is there a printed page/QR visible in frame at all" (the real QR decode stays server-side via pyzbar, `backend/app/routers/scan.py`). `lib/screens/scan/camera_capture_screen.dart` wires this to the `camera` package's captured JPEG bytes and shows an inline retake prompt. Camera hardware isn't available on this dev machine (same constraint as Phase 0's mobile scaffold), so the screen defensively handles `availableCameras()` failing and is verified via `flutter analyze` plus indirect confirmation it renders without crashing (Phase 8.1's real-network test lands on `/scan`); the DoD's actual detection logic, which is what its wording explicitly asks to test ("fed through the same detection logic"), is verified directly in `mobile/test/services/image_quality_test.dart` (4 tests) against in-test-generated fixtures: a sharp synthetic checkerboard page with a QR-finder-style corner pattern does not trigger a retake; the same page run through the `image` package's `gaussianBlur` does; a blank/uniform frame is flagged for having no visible page/QR content; malformed bytes are handled without crashing. — DoD verified.

[Phase 8.3] Local queue + sync: `lib/services/queue_repository.dart` persists the scan queue as one JSON blob (image bytes as base64) in `SharedPreferences`; `lib/services/sync_service.dart` drains it against a `ScanApi` interface (`lib/services/api_client.dart`'s `ApiClient` in production, `POST /scan`) with exponential backoff (2s/4s/8s/... capped at 5 min) on failure, triggered both after every capture and - in production, wired in `main.dart` - on a `connectivity_plus` reconnect event via an injectable `Stream<bool>`, so tests never need real hardware/platform channels. Verified in `mobile/test/services/queue_repository_test.dart` (3 tests: persistence across a simulated restart via a fresh repository instance over the same `SharedPreferences` store, a state-change round-trip, and an exact byte-for-byte image round-trip - this test caught a real bug, `loadAll()` returning a fixed-length list that broke `add()`'s `..add(item)`, fixed by making it growable) and `mobile/test/services/sync_service_test.dart` (5 tests against a `FakeScanApi`: offline items stay queued without touching the network; a successful sync marks an item processed with the real result fields; a failed upload becomes `failed` with a future `nextAttemptAt` and is *not* dropped - re-verified it's skipped before that time and retried with a larger backoff after; and a `StreamController<bool>` connectivity event auto-drains a fully offline 2-item queue with zero explicit sync calls from the test). — DoD verified.

[Phase 8.4] Batch scan summary: `lib/models/batch_summary.dart`'s pure `BatchSummary.fromItems()` tallies the local queue (kept separate from the widget specifically so it's independently verifiable); `lib/screens/scan/batch_summary_screen.dart` displays total/finalized/needs_review/pending/failed plus a textual hand-off to the web app for review (mobile implements no review UI itself, per CLAUDE.md's phase split). DoD ("counts exactly match the actual backend submission statuses...verified against seeded/test data") verified in `mobile/test/scan/batch_summary_backend_test.dart` against the real backend: a real quiz+version is created via raw HTTP (quiz creation is intentionally out of mobile's own `ApiClient` scope), 3 submissions with known statuses (1 finalized, 2 needs_review) are seeded directly via SQL (mirrors `web/test/results_dashboard_test.dart`'s pattern, since no endpoint creates a submission except the real OMR pipeline), each is independently re-fetched via the real `GET /submissions/{id}` endpoint, and `BatchSummary.fromItems()` over the resulting queue items matches the seeded counts exactly. Also covered by `mobile/test/models/batch_summary_test.dart` (2 pure unit tests: mixed states, and the empty-queue edge case). — DoD verified.

[Phase 8] All four subtasks' DoD boxes verified above via real `flutter test` runs against the real local Supabase + FastAPI stack for the auth/backend-dependent tests, and pure unit tests for the deterministic logic (image quality, queue persistence, sync/backoff, batch tally) - `mobile/test/{auth/login_flow,services/image_quality,services/queue_repository,services/sync_service,models/batch_summary,scan/batch_summary_backend}_test.dart`, 17 tests total, all passing individually and together in one `flutter test` run. `flutter analyze` clean (no issues). New dependencies added to `mobile/pubspec.yaml`: `supabase_flutter`, `flutter_riverpod` (pinned to the 2.x major web already uses, not the 3.x `flutter pub add` resolved by default), `go_router` (likewise pinned to 14.x to match web), `http`, `shared_preferences`, `image`, `connectivity_plus`, `camera` (runtime); `mocktail`, `fake_async` (dev, reserved for future use - not yet needed by these tests). Assumption, noted here per CLAUDE.md Section 6: this dev machine has no Android/iOS SDK or emulator (same as Phase 0), so the camera capture screen's actual camera preview/capture flow is verified only via `flutter analyze` plus indirect confirmation it doesn't crash without hardware; the DoD's blur/retake detection logic itself is fully verified in isolation, per the DoD's own wording. Second assumption: because `lib/main.dart`/`lib/router/app_router.dart` necessarily wire all four screens together to form one working app, Phase 8's implementation files land together while each subtask's dedicated test file(s) - the artifact that actually proves that subtask's DoD - get their own commit citing that subtask, rather than four independently-buildable checkpoints. PROGRESS.md updated, code committed per subtask.

[Phase 9 note] "Real (test/staging) Supabase + backend deployment" is interpreted consistently with every prior phase's own established precedent (Phases 4, 6, 7, 8 in this log): the real local Supabase + Postgres + Storage stack this project already runs its entire test suite against, not a separately hosted cloud staging environment - no such environment exists yet (that's Phase 10's job) and CLAUDE.md Section 6 doesn't ask for one here. Flagged as an assumption per the "note it, don't stop" rule rather than a blocker.

[Phase 9.1] Added `backend/tests/test_e2e_happy_path.py`: real HTTP flow through the actual running FastAPI app (`fastapi.testclient.TestClient`, no mocks) - `POST /quizzes` (real Supabase Auth professor) -> `POST /quizzes/{id}/upload` (real `valid.xlsx` fixture, 3 questions) -> `POST /quizzes/{id}/versions` (count=3) -> for each of the 3 real, independently-shuffled versions: `GET /versions/{id}/pdf`, download the real signed URL's bytes, rasterize with PyMuPDF, mark bubbles, `POST /scan`. Two versions are filled entirely correct (expected score 3.0, the max); the third has exactly one question deliberately marked wrong (expected score 2.0) - a deliberate design choice over "mark everything correct on all three", since an all-correct-only test could pass even with a subtly-broken (but symmetric) un-shuffle step; differentiated per-version scores actually exercise the score math. Every assertion is against both the live HTTP response body and a fresh DB re-read (`submissions`/`answers` rows), not response-body-only. Verified: `pytest tests/test_e2e_happy_path.py` passes (1 test, ~15s) against the real local Supabase+Postgres+Storage stack; `ruff check`/`black --check` clean; full backend suite (130 tests) still green with this test included. — DoD verified.
